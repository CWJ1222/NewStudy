package com.shinhan.day04;


//class는 설계도
//구성요소 : field, method 
//object를 만들기 위해 만듬, 실체(instance), class를 이용해서 생성(new)


public class Person {
	
	//1.필드 field : 속성, 변수선언, object들의 공통특성
	String name;
	int age;
	
	//2. 생성자 constructor ...new를 수행 시 자동으로 호출되는 함, class이름과 같아야 
	//정의하지 않으면 compile시 자동으로 만들어짐.
	//return 가질 수 없음/ return이 있으면 일반 함수임.
	Person(){
	System.out.println("Person class의 생성자이다. new시 마다 실행");
	}
	
	//3. method
	//문장들의 묶음, 함수는 반드시 return타입을 정의해야
	void study(){
		System.out.println(name + "study");
	}
	
	//4. block
	
	
	//5. inner class ... 5가지만 올수있다.
	
	//5가지 중 필수적으로 해야 될 건 없음
	
}
